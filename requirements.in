# requirements.in — 直接依赖（source constraints）
# 使用 pip-compile 从此文件生成锁定的 requirements.txt：
#   pip install pip-tools
#   pip-compile --generate-hashes requirements.in
#
# 运行时依赖（仅列出直接依赖，传递依赖由 pip-compile 自动解析并锁定）

Flask>=3.0,<4.0
PyJWT>=2.8,<3.0
structlog>=24.1,<26.0

# Observability: Prometheus metrics export
prometheus-client>=0.20,<1.0

# Observability: OpenTelemetry tracing (optional — gracefully degraded if absent)
opentelemetry-sdk>=1.24,<2.0
opentelemetry-instrumentation-flask>=0.45b0,<1.0

# Orbital mechanics: SGP4/SDP4 propagator for TLE-driven LEO/MEO satellites
sgp4>=2.22,<3.0
