# Development / testing dependencies (not required at runtime)
# Install with: pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0
pytest-benchmark>=4.0,<5.0
hypothesis>=6.100,<7.0

# Code style / linting (kept in sync with quality.yml)
ruff>=0.4,<1.0
black>=24.0,<25.0

# Static type checking
mypy>=1.10,<2.0

# Load / stress testing
locust>=2.20,<3.0
